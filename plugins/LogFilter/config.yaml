name: Log Filter
description: Filter what goes into the activity log
version: 0.5.3

config_settings:
    LogExclude:
        default: ''
    LogExcludeApp:
        default: ''

init: $LogFilter::LogFilter::Util::init
init_app: $LogFilter::LogFilter::Util::init_app
